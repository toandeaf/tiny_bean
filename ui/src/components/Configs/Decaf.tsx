import './Configs.css';
import ConfigContainer from './ConfigContainer';

const Decaf = () => {
  return (
    <ConfigContainer title={'Decaf'}>
      <button>Non-decaf</button>
      <button>Decaf</button>
    </ConfigContainer>
  );
};

export default Decaf;
