import './Configs.css';
import ConfigContainer from './ConfigContainer';

const Size = () => {
  return (
    <ConfigContainer title={'Size'}>
      <button>Large</button>
      <button>Medium</button>
      <button>Small</button>
    </ConfigContainer>
  );
};

export default Size;
